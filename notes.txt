To Do:

1. Add Dropdown menu (react-bootstrap) for Calendar types ie AddEvent component
          <label htmlFor='calendar'>Calendar</label>
          <DropdownButton id="dropdown-basic-button" title="Dropdown button">
              <Dropdown.Item href="#/action-1">Action</Dropdown.Item>
              <Dropdown.Item href="#/action-2">Another action</Dropdown.Item>
              <Dropdown.Item href="#/action-3">Something else</Dropdown.Item>
            </DropdownButton>

            create Dropdown.item dynzmically

              // createDropdownItem: function (image) {
  //   // return <Image source={image} key={image} />;
  //   return <Dropdown.Item href="#/action-1">Action</Dropdown.Item>;
  // },

  // initCalendars: function (images) {
  //   return images.map(this.createImage);
  // },

  old:

  <SourceSelectMenu name='calendar' disabled onChange={this.handleChange} value={this.state.formValues.source} />

  2. Refactor Mongoose code
  3. Replace all 'require's with 'import'